#include "uart_driver.h"
#include "delay_api.h"
#include "lcd_api.h"
#include <stdio.h>

int main() {
	lcd_init();
	lcd_print_string("Welcome!");
	delay_ms(1000);
	lcd_clear();
	lcd_home();
	int start_num = 10;
	for (int i = 0; i < 10; i++) {

	}
	while(1);
	return 0;
}
