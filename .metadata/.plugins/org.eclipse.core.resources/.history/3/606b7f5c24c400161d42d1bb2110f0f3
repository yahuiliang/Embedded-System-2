#include "uart_driver.h";
#include "delay_api.h";
#include <stdio.h>;

int main() {
	init_usart2(19200, 16000000UL);
	int a[] = {1, 2, 3, 4, 5, 156};
	int size = sizeof(a);
	char word[] = "Word";
	printf(word);
	return 0;
}
